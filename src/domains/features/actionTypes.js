const prefix = 'mule-match/features';

const actionTypes = {
  FETCH_FEATURES: `${prefix}/FETCH_FEATURES`,
  SELECT_FEATURE: `${prefix}/SELECT_FEATURE`,
  IGNORE_FEATURE: `${prefix}/IGNORE_FEATURE`
};

export default actionTypes;
